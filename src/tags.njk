---
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - nav
    - post
    - posts
    - tagList
    - catList
layout: page.njk
renderData:
  title: Tagged “{{ tag }}”
permalink: /tags/{{ tag }}/
---
{% from "macros.njk" import articlesList  %}

<h1>Tagged “{{ tag }}”</h1>

{{ articlesList(collections[ tag ]) }}

<p>See <a href="/tags/">all tags</a>.</p>
