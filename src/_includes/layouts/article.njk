---
layout: layouts/base.njk
templateClass: tmpl-post
---
{% from "macros.njk" import timeTag  %}

<h1>{{ title }}</h1>
<div class="article-date">{{ timeTag(page.date)}}</div>

{{ content | safe }}

<section class="article-footer">
  <a href="{{ '/articles' | url }}">← back to articles</a>
</section>
