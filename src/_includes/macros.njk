{%-macro articlesList(aList) -%}
<div class="articleList">
  {%-for article in aList | reverse -%}
    <div class="card">
      <div class="card-title">
        <a href="{{ article.url | url }}">
          <span>
            {%-if article.data.title -%}
              {{ article.data.title }}
            {%-else -%}
              <code>{{ article.url }}</code>
            {%-endif -%}
          </span>
        </a>
      </div>
      <div class="card-subhead">
        <a href="{{ article.url | url }}">
        <span>
          {{- article.data.subhead | safe -}}
        </span>
        </a>
      </div>
      <div class="card-category">
          <a class="category" title="{{article.data.category}}" href="/categories/{{article.data.category}}">
            {{- article.data.category.substring(0,2) -}}
          </a>
      </div>
      <div class="card-tags">&#8203;
        {{- taglist(article.data.tags) -}}
      </div>
      <div class="card-date">{{ timeTag(article.date, "")}}</div>
    </div>
  {%-endfor -%}
</div>
{% endmacro %}

{% macro timeTag(theDate, theClass) %}
  <span {%-if theClass -%}class="{{- theClass -}}"{%-endif -%}><time
        datetime="{{ theDate | htmlDateString }}">
    {{ theDate | readableDate }}
  </time></span>
{%endmacro %}


{# sort(reverse, caseSens) #}

{%- macro taglist(theTags) -%}
  {%- for tag in theTags | sort(false, true) %}
    {%- set tagUrl %}/tags/{{ tag }}/{%- endset -%}
    <a href="{{ tagUrl | url }}" class="tag"><span>{{ tag }}</span></a> <!-- -->
  {%- endfor %}
{%- endmacro -%}
